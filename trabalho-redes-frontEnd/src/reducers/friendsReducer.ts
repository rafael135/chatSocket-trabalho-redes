import { ActionType } from "@/types/Reducer";
import { UserFriend } from "@/types/User";

export default function friendsReducer(friends: UserFriend[], action: ActionType) {
    switch(action.type) {
        
    }
}